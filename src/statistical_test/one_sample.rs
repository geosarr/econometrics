/// Gauss-Test.
///
/// Assuming the samples X<sub>1</sub>, . . . , X<sub>n</sub> ∼ N(μ,
/// σ<sup>2</sup>) are i.i.d., where N(μ, σ<sup>2</sup>) is the gaussian law
/// with mean μ and **known variance** σ<sup>2</sup>. This function can test the
/// following:
///
/// (a) H0 : μ ≤ μ<sub>0</sub> vs. H1 : μ > μ<sub>0</sub> (upper one-sided
/// test).
///
/// (b) H0 : μ ≥ μ<sub>0</sub> vs. H1 : μ < μ<sub>0</sub> (lower one-sided
/// test).
///
/// (c) H0 : μ = μ<sub>0</sub> vs. H1 : μ != μ<sub>0</sub> (two-sided test).
pub fn ztest() {}
